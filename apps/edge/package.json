{
  "name": "@burrow/edge",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc -p tsconfig.json",
    "test": "echo 'no tests'",
    "db:migrate": "wrangler d1 migrations apply adabuild-db --remote",
    "db:migrate:local": "wrangler d1 migrations apply adabuild-db --local"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260813.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.122.0"
  }
}
